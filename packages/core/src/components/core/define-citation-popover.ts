import { LoquixCitationPopover } from './loquix-citation-popover.js';

if (!customElements.get('loquix-citation-popover')) {
  customElements.define('loquix-citation-popover', LoquixCitationPopover);
}
